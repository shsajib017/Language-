#include<stdio.h>

int main(){

    // array = a data structure that can store many values of the same data types.
   
     float prices[] = {5.0, 10.0, 15.0, 20.0, 25.0};

     printf("$%.2f\n", prices[0]);
     printf("$%.2f\n", prices[1]);
     printf("$%.2f\n", prices[2]);

     return 0;
}